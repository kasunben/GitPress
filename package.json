{
  "name": "gitpress",
  "version": "1.0.0",
  "description": "GitPress is a **minimal, Git-backed Markdown CMS**.   The goal is to let non-technical users create, edit, and publish blog posts stored in a GitHub repository, without ever touching Git directly.",
  "main": "index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "simple-git": "^3.28.0"
  }
}
